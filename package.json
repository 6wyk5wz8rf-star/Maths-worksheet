{
  "name": "maths-page-studio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A local-first question-to-worksheet composer for primary mathematics.",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "test": "node --test",
    "serve": "python3 -m http.server 4173"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
